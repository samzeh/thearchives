# model-backend

## Setup

```bash
cd /Users/samuelhuang/thearchives/model-backend
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install --upgrade pip
python3 -m pip install -r requirements.txt
```

## Run (FastAPI CLI)

```bash
fastapi dev app.py
```
